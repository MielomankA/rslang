export interface IAdvantageCardProps {
  title: string;
  description: string;
}

export interface IDeveloperCardProps {
  avatarUrl: string;
  name: string;
  githubUrl: string;
  jobStatus: string;
  description: string;
}
